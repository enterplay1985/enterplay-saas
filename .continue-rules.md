# Reglas Críticas del Proyecto
1. **Estructura Sagrada**: El archivo `src/app/page.tsx` contiene el Dashboard profesional. ESTÁ PROHIBIDO borrar su diseño, componentes de gráficas o estructura Tailwind.
2. **Modo Incremental**: Si necesitas añadir una funcionalidad, agrégala como un componente nuevo o una línea extra. NUNCA reemplaces el archivo completo por una versión simplificada.
3. **Preservación de Lógica**: No elimines importaciones ni funciones de fetching de Airtable (USR-001) a menos que se pida explícitamente.
4. **Confirmación**: Antes de realizar un cambio en `page.tsx` o `layout.tsx`, describe qué vas a hacer y espera mi "OK".